/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "images.unsplash.com",
      "https://lh3.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
